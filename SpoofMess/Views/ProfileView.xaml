<UserControl x:Class="SpoofMess.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpoofMess.Views"
             xmlns:vm="clr-namespace:SpoofMess.ViewModels"
             xmlns:ve="clr-namespace:SpoofMess.ViewElements"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProfileViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DockPanel>
            <Button Style="{DynamicResource FunctionButton}" Content="&lt;" DockPanel.Dock="Left"/>
            <Button Style="{DynamicResource FunctionButton}" Content="❌" DockPanel.Dock="Right"/>
            <TextBlock Style="{DynamicResource PrimaryText}" HorizontalAlignment="Left" FontWeight="Bold" Text="Profile"/>
        </DockPanel>
        <StackPanel Grid.Row="2">
            <ve:InputTextBox Holder="Name" Text="{Binding UserInfo.Name}"/>
            <ve:InputTextBox Holder="Login" Text="{Binding UserInfo.Login}"/>
            <ve:FunctionButton Text="Save"/>
        </StackPanel>
    </Grid>
</UserControl>
