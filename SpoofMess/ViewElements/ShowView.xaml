<UserControl x:Class="SpoofMess.ViewElements.ShowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpoofMess.ViewElements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Name="ShowViewView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Visibility="{Binding ElementName=ShowViewView, Path=HeaderVisibility}">
            <local:FunctionButton Text="❌" DockPanel.Dock="Right"/>
            <TextBlock Text="{Binding ElementName=ShowViewView, Path=Header}" DockPanel.Dock="Left"/>
        </DockPanel>
        <ContentPresenter Grid.Row="1" Content="{Binding ElementName=ShowViewView, Path=View}"/>
        
    </Grid>
</UserControl>
