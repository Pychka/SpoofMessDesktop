<UserControl x:Class="SpoofMess.ViewElements.InputTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpoofMess.ViewElements"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200" x:Name="InputTextBoxView">
    <Grid>
        <TextBlock VerticalAlignment="Center" Margin="5 0 0 0" FontSize="20" Foreground="Gray" Background="White" x:Name="HolderText" Text="{Binding ElementName=InputTextBoxView, Path=Holder}"/>
        <TextBox Background="Transparent" Text="{Binding ElementName=InputTextBoxView, Path=Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="InputTextChanged" x:Name="Input"/>
    </Grid>
</UserControl>
